; Тест загрузки константы (A=1, B=155)
LOAD #155

; Тест чтения из памяти (A=7)
; Предполагаем, что по адресу 100 лежит значение 42
LOAD #100  ; Загружаем адрес в стек
LOAD       ; Читаем значение из памяти по адресу из стека

; Тест записи в память (A=4, B=463)
LOAD #99   ; Загружаем значение для записи
STORE 463  ; Записываем значение 99 по адресу 463

; Тест сложения (A=3)
; Предположим: memory[200] = 10, в стеке 20
LOAD #200  ; Загружаем адрес первого операнда
LOAD #20   ; Загружаем второй операнд
ADD        ; Складываем: 10 + 20 = 30
